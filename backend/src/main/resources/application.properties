# Server Configuration
server.port=8080
spring.application.name=ai-context-blog

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/ai_context_blog?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=kalpak

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000

# JWT Configuration
jwt.secret=your-secret-key-change-this-in-production-use-a-long-random-string
jwt.expiration=86400000

# OpenRouter API Configuration
openai.api.key=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
openai.api.base-url=https://openrouter.ai/api/v1

# CORS Configuration
cors.allowed-origins=http://localhost:3000

# Disable H2 Console (using MySQL)
spring.h2.console.enabled=false

